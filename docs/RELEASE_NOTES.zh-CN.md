# 发布说明（中文）

## 发布范围

本次发布主要用于将仓库整理为适合公开开源的版本。

## 变更内容

- 清理顶层目录结构，并将探索性脚本迁移到 `legacy/`。
- 保留 `final/` 作为当前发布基线代码。
- 新增统一命令行入口 `scripts/run.py`。
- 为维护中的算法统一补充随机种子、重复次数和无图模式参数。
- 新增中日英三语配套文档。
- 新增最小 GitHub Actions 流程，用于依赖安装与语法检查验证。
- 将根目录中的示意图整理到 `docs/assets/` 作为文档资源。
- 在发布前再次检查常见敏感信息模式。

## 建议提交说明

```text
chore: 整理开源发布版本，补充统一 CLI、CI 与三语文档
```
