# Legacy Experiments

This directory contains historical exploratory scripts that were moved out of the repository root during open-source release cleanup.

These files are preserved for traceability and idea history, but they are not the recommended implementation baseline.

For reproducible runs and reporting, use scripts under `final/`.
